# Quick Start

```bash
chmod +x start.sh
./start.sh
```

Open:

```text
http://<host-ip>:6767
```

Login with:

```text
thoovara@hari
```

Use auto-select for normal chat. Phi-3 remains the default and handles most requests.
