# Production Quick Start

For a LAN-only deployment:

```bash
./start.sh
```

Keep the host on AC power, keep Ollama models limited to the approved list, and prefer auto-select so Phi-3 handles ordinary prompts.

Do not expose port `6767` to the public internet without adding stronger authentication and TLS.
